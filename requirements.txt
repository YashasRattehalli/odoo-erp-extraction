# ---------------------------------------------------------------------------
# Runtime dependencies for the Odoo ERP Extraction tool.
# Pinned to compatible-release ranges that are known-good on Python 3.11–3.13.
# ---------------------------------------------------------------------------

# Browser automation
playwright>=1.44,<2.0

# PHI / PII detection & anonymization (in-memory log sanitization)
presidio-analyzer>=2.2.355,<3.0
presidio-anonymizer>=2.2.355,<3.0
spacy>=3.7,<3.9            # NLP engine backing Presidio (en_core_web_sm model)
click>=8.1,<9.0           # required by spaCy's CLI (not always pulled in by typer>=0.26)

# Configuration
pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0
python-dotenv>=1.0,<2.0

# Premium PDF specification rendering
weasyprint>=60
jinja2>=3.1,<4.0
pygments>=2.17,<3.0
